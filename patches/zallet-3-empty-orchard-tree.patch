diff --git a/zallet/src/components/sync/steps.rs b/zallet/src/components/sync/steps.rs
index 76e31bb..c8c158f 100644
--- a/zallet/src/components/sync/steps.rs
+++ b/zallet/src/components/sync/steps.rs
@@ -325,20 +325,17 @@ pub(super) async fn fetch_chain_state(
         Frontier::empty()
     };
 
-    let final_orchard_tree = if params.is_nu_active(NetworkUpgrade::Nu5, height.0.into()) {
-        read_frontier_v0(
-            orchard
-                .ok_or_else(|| IndexerError::InvalidData {
-                    message: "Missing Orchard tree state".into(),
-                })?
-                .as_slice(),
-        )
-        .map_err(|e| IndexerError::InvalidData {
-            message: format!("{e}"),
-        })?
-    } else {
-        // NU5 is not yet active; the Orchard tree is empty.
-        Frontier::empty()
+    let final_orchard_tree = match orchard {
+        // NU5 is active and the indexer returned an Orchard tree state: read its frontier.
+        Some(orchard) if params.is_nu_active(NetworkUpgrade::Nu5, height.0.into()) => {
+            read_frontier_v0(orchard.as_slice()).map_err(|e| IndexerError::InvalidData {
+                message: format!("{e}"),
+            })?
+        }
+        // Either NU5 is not yet active, or NU5 is active but the Orchard tree is still
+        // empty -- the indexer returns no tree state for an empty tree (cf. the Sapling
+        // path, which returns an empty frontier). Treat both as an empty Orchard tree.
+        _ => Frontier::empty(),
     };
 
     Ok(ChainState::new(
